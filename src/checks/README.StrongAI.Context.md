<!-- Generated by StrongAIAutoDoc 20260523 -->

Ripstop Guardrails is a TypeScript-based suite of Git hook and CI checks that prevents risky repository changes and enforces policy before code is accepted. It runs in local developer workflows (pre-commit, commit-msg, pre-push, pre-rebase) and in CI, analyzing staged files, diffs, commit messages, and push metadata. It also snapshots configuration state for later forensic recovery. The system interacts primarily with Developers, CI pipelines, Git clients/hooks, the local repository filesystem, and Git itself to determine branch context and validate push operations.

```mermaid
C4Context
title Ripstop Guardrails - Context Diagram

Person(dev, "Developer", "Runs Git commands; receives guardrail feedback in hooks")
Person(ciUser, "CI Pipeline", "Executes checks in automated builds and pull requests")

System_Boundary(sb, "Ripstop Guardrails (this directory)") {
  System(ripstop, "Ripstop Guardrails Checks", "TypeScript check suite selecting and running policy checks across Git hooks and CI")
}

System_Ext(git, "Git (Client & Hooks)", "Invokes pre-commit, commit-msg, pre-push, pre-rebase; provides hook inputs and repository state")
System_Ext(repoFs, "Repository Working Copy & Filesystem", "Source files, diffs, .guardrails.yaml, RIPSTOP.md, witness/audit logs")
System_Ext(ci, "CI Platform", "Runs checks in CI mode with repository checkout and configuration")
System_Ext(node, "Node.js Runtime", "Executes TypeScript/JS checks and provides standard libraries")
System_Ext(globLib, "picomatch", "Glob matching for protected paths/branches and exemptions")
System_Ext(schemaLib, "zod", "Configuration schema validation and defaults")
System_Ext(osCrypto, "Crypto (SHA-256)", "Hashing for configuration and RIPSTOP.md witness records")

Rel(dev, git, "Performs commits/pushes/rebases that trigger hooks", "CLI")
Rel(ciUser, ci, "Triggers builds that run guardrails", "CI job")

Rel(git, ripstop, "Invokes checks with trigger context (files, commit message, pre-push stdin)", "Hook execution")
Rel(ci, ripstop, "Invokes checks in CI mode over checked-out repo", "Job execution")

Rel(ripstop, repoFs, "Reads files/diffs and writes witness/audit records", "Filesystem I/O")
Rel(ripstop, git, "Queries branch and evaluates push updates", "git commands & hook payload")
Rel(ripstop, node, "Runs under", "Runtime")
Rel(ripstop, globLib, "Matches branch/path globs and exemptions", "Library")
Rel(ripstop, schemaLib, "Validates/normalizes per-check configuration", "Library")
Rel(ripstop, osCrypto, "Computes hashes for config and RIPSTOP.md state", "Library")
```

### Key components and external interactions
The system is invoked by Git hooks and CI, then uses a registry to select checks appropriate for each trigger (pre-commit, commit-msg, pre-push, pre-rebase, CI). It reads repository content and metadata from the filesystem (changed files, optional diffs, .guardrails.yaml, RIPSTOP.md) and emits findings as warnings or errors depending on enforce mode. HistoryGuard consumes pre-push stdin to detect protected-branch deletions and force updates. PathGuard validates edits to protected paths and inspects commit message trailers for approvals. PII and TestSkip scan file contents or added diff lines. ReflogWitness snapshots configuration hashes (and optionally content) and records them to a witness log, querying Git for branch context. Configuration parsing depends on zod, while glob matching relies on picomatch, and hashing uses Node crypto.