repo:
  name: unknown
  domain: network
  tier: 2

extends: "@jonverrier/ripstop/presets/telco-generic"

checks:
  ripstop-md-fresh:
    mode: enforce
    triggers: [pre-commit, ci]
    output_path: "RIPSTOP.md"
