<!-- Generated by StrongAIAutoDoc 20260523 -->

This directory implements Ripstop’s reporting layer for CLI check runs. It defines the common data model and reporter contract, selects an output format, and provides two concrete renderers. The human reporter produces concise, line-oriented terminal text suitable for developers and CI logs, while the JSON reporter emits a stable, machine-readable serialization for automation. Together, these modules standardize how findings and summary counts are printed to standard output.

```mermaid
C4Component
title Ripstop Reporting Modules (TypeScript)

Component(reporter, "Reporter.ts", "TypeScript Module", "Defines IRunReport/IReporter and createReporter(format) factory. Imports findings type; instantiates concrete reporters.")
Component(human, "human.ts", "TypeScript Module", "HumanReporter: renders a run report as readable, line-oriented text to stdout.")
Component(json, "json.ts", "TypeScript Module", "JsonReporter: serializes a run report as pretty-printed JSON to stdout.")

Component_Ext(checkTypes, "Checks Types (IFinding)", "Type Definitions", "Shared finding type definitions used across the checking subsystem.")
System_Ext(stdout, "process.stdout", "Node.js Runtime", "Standard output stream for CLI/log emission.")
System_Ext(cli, "Ripstop CLI / Runner", "External Caller", "Invokes createReporter and passes IRunReport to reporter.write().")

Rel(cli, reporter, "Calls createReporter(format) and uses returned IReporter")
Rel(reporter, human, "Creates instance when format is human/default")
Rel(reporter, json, "Creates instance when format is json")
Rel(reporter, checkTypes, "Imports IFinding for typed IRunReport.findings")
Rel(human, reporter, "Implements IReporter; consumes IRunReport types")
Rel(json, reporter, "Implements IReporter; consumes IRunReport types")
Rel(human, stdout, "Writes formatted text")
Rel(json, stdout, "Writes JSON string + newline")
```

### Key components
**Reporter.ts** is the directory’s hub: it defines **IRunReport** (findings plus enforced failure/warning counts) and the **IReporter** interface, then exposes **createReporter(format)** to choose an implementation. It also imports **IFinding** from the checks type definitions to keep report payloads consistent across the system. **HumanReporter (human.ts)** implements IReporter by converting each finding into a readable block (severity, check name, location, message, rule) and printing a final summary. **JsonReporter (json.ts)** implements IReporter by emitting the entire report via `JSON.stringify` with indentation. Both reporters write to **process.stdout**.