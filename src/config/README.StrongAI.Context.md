<!-- Generated by StrongAIAutoDoc 20260523 -->

Ripstop configuration tooling loads, validates, merges, and fingerprints repository configuration so automation can reliably decide whether documentation such as RIPSTOP.md is up to date. It is used by developers and CI pipelines that need deterministic results across machines and runs. The system reads YAML configuration files from a repository, may inherit from built-in preset YAMLs, validates using schemas, and produces a stable SHA-256 hash for change detection.

```mermaid
C4Context
title Ripstop Config Tooling - Context Diagram

Person(dev, "Developer", "Edits ripstop YAML config and expects consistent validation and merging.")
Person(ci, "CI Pipeline", "Runs automated checks and uses config hash to verify RIPSTOP.md freshness.")

System_Boundary(ripstop, "Ripstop Config Tooling") {
  System(tooling, "Config Loader/Validator/Hasher", "Loads YAML, resolves preset inheritance, deep-merges, validates via Zod schema, and computes deterministic SHA-256 hash.")
}

System_Ext(fs, "File System", "Reads repository config YAML and preset YAML files.")
System_Ext(presets, "Built-in Preset Packages", "Preset YAMLs referenced via @jonverrier/ripstop/presets/*.")
System_Ext(yaml, "js-yaml", "Parses YAML into JS objects.")
System_Ext(zod, "Zod", "Validates and normalizes config via schemas.")
System_Ext(nodeCrypto, "Node.js crypto", "Computes SHA-256 digest for stable config hashing.")
System_Ext(nodePath, "Node.js path", "Resolves and normalizes file paths safely.")
System_Ext(fsPromises, "Node.js fs/promises", "Async file access for reading config and presets.")

Rel(dev, tooling, "Runs locally to load/validate config and compute hash")
Rel(ci, tooling, "Runs in CI to enforce config validity and detect doc staleness")
Rel(tooling, fsPromises, "Reads YAML files", "async I/O")
Rel(tooling, nodePath, "Resolves config paths and preset references")
Rel(tooling, yaml, "Parses YAML into objects")
Rel(tooling, zod, "Validates against RipstopConfigSchema and applies defaults")
Rel(tooling, presets, "Resolves extends to built-in presets", "restricted prefix")
Rel(tooling, nodeCrypto, "Hashes canonicalized config", "SHA-256 hex")
Rel(tooling, fs, "Accesses repository working tree", "config + documentation files")
```

Key components and external interactions center on reliably turning YAML into a validated, typed configuration and a deterministic fingerprint. The tooling reads repository YAML through Node.js fs/promises and resolves paths with Node.js path to avoid unsafe or inconsistent resolution. YAML parsing is delegated to js-yaml, and structural validation plus defaults are enforced with Zod schemas to produce an IRipstopConfig. Preset inheritance is supported by recursively loading built-in preset YAMLs (restricted to the @jonverrier/ripstop/presets/ prefix) and deep-merging with repository overrides. Finally, Node.js crypto computes a SHA-256 hash over a canonical, stable string form for freshness checks.